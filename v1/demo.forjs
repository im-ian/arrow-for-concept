for (1 -> 3) {
  console.log('tick'); // 카운터 없이 3회 반복
}

const max = 3;
for (let b = max -> max + 2) {
  console.log('expr bound:', b);
}

for (let step = 1 -> 3) {
  console.log('named:', step);
}

for (let count = 3 -> 1) {
  console.log('countdown:', count);
}
